services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: duocone
    ports:
      - "5432:5432"
    volumes:
      - duocone_db:/var/lib/postgresql/data

volumes:
  duocone_db:
